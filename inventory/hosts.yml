---
all:
  children:

    linux:
      hosts:
        # Add your Linux hosts here
        # linux-server-01:
        #   ansible_host: 192.168.1.10
        #   ansible_user: ansible
        #   ansible_ssh_private_key_file: ~/.ssh/id_rsa

    windows:
      hosts:
        # Add your Windows hosts here
        # win-server-01:
        #   ansible_host: 192.168.1.20
        #   ansible_user: DOMAIN\ansible
        #   ansible_password: "{{ vault_win_password }}"

    paloalto:
      hosts:
        # Add your Palo Alto firewalls here
        # pa-fw-01:
        #   ansible_host: 192.168.1.1
        #   paloalto_username: admin
        #   paloalto_password: "{{ vault_pa_password }}"

    # Dynamic target group populated at runtime by alert_host variable
    alert_target:
      hosts: {}
